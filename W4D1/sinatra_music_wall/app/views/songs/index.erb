<h1>Song Wall</h1>

<% @songs.each do |song| %>
  <div class="song">
  <a href="songs/<%= song.id %>"><%= song.title %></a> by <%= song.author %>
  </div>
<% end %>

<a href="/songs/new">Add a New Song</a>